rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /users/{uid}/{allPaths=**} {
      allow read: if request.auth != null && request.auth.uid == uid;
      // If only server/admin uploads, keep write off:
      allow write: if false;
    }
  }
}
